DB_URL=postgresql://user:password@host/dbname?sslmode=require
BEARER_TOKEN_X=
NEWS_API_KEY=
CORS_ORIGINS=https://netalens.up.railway.app,http://localhost:3000,http://127.0.0.1:3000
REDIS_URL=
CLIENT_ID=
CLIENT_SECRET=
REDIRECT_URI=
# Date the political dataset is published as current for (YYYY-MM-DD).
# Unset falls back to the newest ingestion timestamp in the data.
DATA_UPDATED_AT=2026-07-30
